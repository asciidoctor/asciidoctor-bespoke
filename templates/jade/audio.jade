audio(id=id src=(node.getMediaURI(getAttribute('target'))), loop=node.$option('loop', ''), preload='auto'
    data-rewind=node.$option('rewind', '') data-volume=(node.$option('muted') ? 0 : node.getAttribute('volume')))
  span.alt=getAttribute('alt', 'Your browser does not support the audio element.')
